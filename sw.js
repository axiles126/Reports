// Копібара — service worker. Оновлення КЕРОВАНЕ: нова версія чекає згоди,
// застосунок показує запит «є оновлення» з переліком змін і оновлюється лише за натиском.
const VERSION = '2026.07.25';
const NOTES = [
  'Кнопка «Інструкція» вгорі зліва: детальний гайд зі складання та надсилання звіту',
  'Нові іконки: взліт — стрілка вгору від землі, у польоті — посадка (стрілка вниз)',
  'Іконку застосунку перенесено до кнопки встановлення (ховається, коли застосунок уже встановлено); можна додати за посиланням з інтернету — розмір підганяється автоматично',
  'Власна іконка застосунку: обирається до встановлення (manifest підміняється на льоту)',
  'Поле «Старший групи» — додається окремим рядком у звіт',
  'Редактор звіту (олівець): події доби, ручне редагування вильотів',
  'Звіт «Доба» — хронологія подій з авто-інтервалами «Ведення розвідки»',
  'Налаштування зміни: початок (16:00), шифр району, ручна к-сть вильотів',
  'Кастомні поля вильоту (текст/число/список) з авто-підрахунками',
  'Підсумки за шаблонами коментарів у звітах День/Ніч/7 днів',
  'Зміна «Доба» рахується від початку зміни, День/Ніч — від 06:00',
  'Голос стабільніший (свіжий екземпляр розпізнавання + надійний перезапуск); іконку встановлення замінено на SVG (більше не схожа на «?»)',
  'Прибрано дубль кнопки/вікна довідки (один знак питання); стабільніше голосове розпізнавання (перезапуск)',
  'Кнопка «?» біля мікрофона — вікно з переліком і поясненням голосових команд',
  'Кнопка «?» біля мікрофона — список голосових команд із поясненнями',
  'Голосове керування (Web Speech, укр.): зліт, сів, звіт, копіювати, карта, день/ніч/доба/тиждень, коментар …',
  'Прибрано додавання фото/відео зі звіту',
  'Фото/відео/файл надсилаються РАЗОМ зі звітом; прев’ю вкладення в застосунку',
  'Камера: окремо Фото і Відео (зйомка з телефона); кнопки медіа — іконки без фону й рамок',
  'Звіт за зміну: День (06–18), Ніч (18–06) і Доба (24г) окремо; необов’язкове вкладення фото/відео або камера до звіту',
  'Мапа: підпис азимута (град+тисячні) і дистанції біля конуса, плавний компас; точки — назва/колір/видалення; лінійка між точками',
  'Якість сигналу в конусі за рельєфом і частотою; усі повзунки числом; повзунки сховано в меню; сузір’я Копібара',
  'Кнопка налаштувань (чекбокси: конус/коридор/сітка/небо), азимут числом у градусах і тисячних; сузір’я Велика і Мала Курочка',
  'Коридор висот: мін/макс висота прийому за нахилом і кутом променя (числом + вертикальне вікно у 3D)',
  'Рельєф у 3D тепер повна поверхня на 360° (реальні висоти DEM за координатами), відтінки за висотою',
  'Рельєф: кілька дзеркал DEM + fallback на центр мапи; сузір’я «Курочка» в нічному небі 3D',
  'Показ зворотного азимута (у 3D зверху та в рядку мапи)',
  'У 3D: рельєф+профіль видимості (LOS), висота антени, підписи сторін світу, нічне небо (зорі/місяць); пресети частот 915/2400/5800',
  '3D-перегляд конуса антени (азимут/кут/нахил/дальність, обертання, застосувати до мапи)',
  'Чорно-біла мапа (тайли grayscale), оверлеї лишаються кольоровими',
  'Чіткі SVG-іконки на головній (зліт/посадка/поділитися/карта/звіт/скинути)',
  'Нові SVG-іконки кнопок на головній',
  'Іконка-шеврон «Capibara fly mode»',
  'Звіт можна редагувати в попередньому перегляді перед надсиланням',
  'Кнопки-іконки без підписів, акуратне розташування; вимкнено масштабування пальцями',
  'Кнопки без рамок і фону, з іконками',
  'Виправлено помилку запуску (елемент не знайдено)',
  'Інтерфейс повністю українською',
  'Адаптивний інтерфейс під сучасні екрани (safe-area, масштабування)',
  'Встановлення на будь-який пристрій, з інструкцією для iOS',
  'Оновлені шаблони коментарів (артилерія, fpv, супровід, розвідка тощо)',
  'Зміна та тиждень — список вильотів (взліт–сів, тривалість, коментар)',
  'Перемикач «Показувати конус» на мапі (типово вимкнено)',
  'Сектор антени з урахуванням рельєфу (онлайн)',
  'Нові іконки застосунку',
  'Оновлення тепер за запитом, з переліком змін',
];
const CACHE = 'kapibara-' + VERSION;
const ASSETS = ['./', './index.html', './manifest.webmanifest', './icon-192.png', './icon-512.png'];

self.addEventListener('install', e => {
  // НЕ викликаємо skipWaiting — нова версія лишається у стані waiting до згоди користувача
  e.waitUntil(caches.open(CACHE).then(c => c.addAll(ASSETS)));
});

self.addEventListener('activate', e => {
  e.waitUntil(
    caches.keys()
      .then(keys => Promise.all(keys.filter(k => k !== CACHE).map(k => caches.delete(k))))
      .then(() => self.clients.claim())
  );
});

self.addEventListener('message', e => {
  const d = e.data;
  if (d === 'skipWaiting') { self.skipWaiting(); return; }
  if (d && d.type === 'getVersion' && e.ports && e.ports[0]) {
    e.ports[0].postMessage({ version: VERSION, notes: NOTES });
  }
});

self.addEventListener('fetch', e => {
  const req = e.request;
  if (req.method !== 'GET') return;
  const url = new URL(req.url);
  const sameOrigin = url.origin === location.origin;

  const isDoc = req.mode === 'navigate' ||
                (sameOrigin && (url.pathname.endsWith('/') || url.pathname.endsWith('/index.html')));

  // Документ — З КЕШУ перш за все (кероване оновлення), мережа лише як запас на першому завантаженні.
  if (isDoc) {
    e.respondWith(
      caches.match('./index.html').then(c => c || fetch(req).then(res => {
        const copy = res.clone();
        caches.open(CACHE).then(ch => ch.put('./index.html', copy)).catch(() => {});
        return res;
      }).catch(() => caches.match('./')))
    );
    return;
  }

  if (sameOrigin) {
    e.respondWith(
      caches.match(req).then(cached => {
        const net = fetch(req).then(res => {
          if (res && res.ok) { const copy = res.clone(); caches.open(CACHE).then(c => c.put(req, copy)).catch(() => {}); }
          return res;
        }).catch(() => cached);
        return cached || net;
      })
    );
    return;
  }

  e.respondWith(fetch(req).catch(() => caches.match(req)));
});
